/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        // 라이트(아이보리) 테마 시맨틱 토큰
        ink: '#f6f2e9', // 앱 캔버스(따뜻한 아이보리)
        panel: '#fbfaf4', // 사이드바·헤더(거의 흰색, 살짝 따뜻)
        panel2: '#efebde', // 보조 표면(고스트 버튼·칩 배경)
        edge: '#e4ded0', // 보더(따뜻한 라이트 그레이)
        accent: '#4f46e5', // 강조 텍스트/아이콘(인디고-600)
        accent2: '#6366f1', // 주요 버튼(인디고-500)
        // gray 스케일 반전: 낮은 숫자일수록 어둡게 → 다크용 text-gray-100~600 가
        // 라이트 배경에서 그대로 "어두운→옅은 텍스트"로 동작한다(컴포넌트 무수정).
        gray: {
          100: '#1f2430', // 가장 진한 텍스트
          200: '#2c3243', // 본문 텍스트
          300: '#454c5e', // 강한 보조
          400: '#6a7180', // 라벨/보조
          500: '#8d929e', // 흐린 텍스트
          600: '#b7bcc6', // 가장 흐린/플레이스홀더
          700: '#374151', // (표준 다크, 미사용 대비)
          800: '#1f2937',
          900: '#111827',
        },
      },
    },
  },
  plugins: [],
};
