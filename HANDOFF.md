# HANDOFF

> 살아있는 상태 문서 — **삭제하지 말 것.** 세션 시작 시 CLAUDE.md 워크플로우 규칙대로 정리.
> 상세 이력·완료 내역은 git log가 보존하니 여기엔 남기지 않는다(짧게 유지).

## 🎯 다음 할 일
- [ ] **ESC(게임 중) 메뉴·설정 화면도 이미지화** — 메인 메뉴와 같은 업로드 구조로 확장(슬롯: 기록·저장·불러오기·설정·메인메뉴·돌아가기 + 창/패널 배경 PNG). 사용자가 해당 이미지를 만든 뒤 착수
- [ ] **Typecast 실키 검증**(키 발급 완료) — 앱에 키 입력 후: ① 보이스 목록 자동 로딩+필터(성별/나이/용도) ② ✨ 자연어 추천(한국어/영어 쿼리 — 스키마 방어적 파싱이라 실응답 확인 필요) ③ 미리듣기 1회 생성+캐시 재청취 ④ 💡 견적 ↔ 실제 크레딧 차감 일치 ⑤ 잔여 크레딧 표시(/v1/users/me/subscription) ⑥ 일괄 생성 소량 + 402 가드. 대본 확정 후 3000줄 본생성은 Lite($15/월 20만 크레딧) 전환 검토
- [ ] Vercel 배포 시 api/typecast.ts Edge 함수 배포 확인
- [ ] **표정 AI 배정 실키 검증** — OpenAI 키 넣고 장면 2~3개로: ① 후보 밖 라벨이 안 나오는지 ② 한 대화에서 표정이 매 줄 튀지 않는지(연속성 지시) ③ 미소 계열 6종이 상황에 맞게 갈리는지 ④ 견적 ↔ 실제 토큰 사용량. 실기 스프라이트 24종 업로드 후 실행 스크린샷까지

## 📌 알아둘 것 (지속)
- **Supabase Storage 경고는 무시**(대시보드 "Remove policy" 절대 누르지 말 것 — 에셋 동기화가 400으로 깨진다). 에셋 버킷의 실제 노출 범위는 "배포 URL 아는 사람 = 전부 열람·업로드 가능"이며 감수한 선택(2026-08-05). 상세는 `supabase/setup.sql` 머리 주석.
- 미착수(이번에 의도적으로 뺌): `store.ts`(2073줄) 슬라이스 분리, 탭 컴포넌트 코드 스플리팅.

## ✅ 방금 반영됨 (다음 세션에서 git log 확인 후 이 줄들 삭제)
- 표정 AI 문맥 배정 + 스프라이트 일괄 업로드 — 키워드 휴리스틱을 `resolveEmotion` 단일 판정으로 통합(5곳 중복 제거), `Line.emotionAuto`(AI 배정)를 작가 태그와 분리 저장, 장면 단위 배치 + 시놉시스·직전줄 문맥 + 연속성 지시, 표정별 설명(`expressionNotes`)을 프롬프트에 실음, 견적(API 0회). 파일명 자동 매칭 스프라이트 일괄 업로드(`importSpritesBatch`). 곁다리 버그 2건: `renameExpression`/`removeExpression`이 `outfits[].expressions`를 안 건드려 고아 스프라이트가 GC조차 안 되던 것, 삭제된 표정 이름이 대사에 남던 것. test 301→338 · **회귀 0 실측**(7구성 79파일 바이트 동일) · lint 무경고 · 유령표정 차단 실출력 확인 · 24종 UI 실브라우저 확인(이름 잘림 수정)
- 게임 아이콘 설정 — 에셋 탭 "🪟 게임 아이콘"에 exe 아이콘(.ico → 프로젝트 루트 `icon.ico`)과 창 아이콘(PNG → `config.window_icon`) 업로드 추가. 루트 파일 출력은 `README.md` 선례가 있어 배관 불필요, 폴더 동기화는 `game/`만 비우므로 해제 전파용 `removeEntry('icon.ico')` 추가. `.ico` 는 MIME 미등록 시 `File.type` 이 빈 문자열이라 확장자 폴백 필요(`uploadAsset` opts.mime), 내보내기 확장자도 png→ico 교정. test 292→301 · **회귀 0 실측**(6구성 68파일 바이트 동일) · lint 무경고 · **실행 검증에서 `gui.window_icon` 이 무시된다는 걸 발견해 `config.window_icon`(options.rpy)으로 수정** · 사용자 .ico 를 Ren'Py 자체 파서로 파싱 확인(6크기)
- 인게임 우측 퀵메뉴 이미지 GUI — 메인 메뉴와 같은 업로드 구조로 확장(10슬롯 × 기본/마우스오버/비활성화 + 스킵·자동 활성화, 보조 패널, 좌표 조절·미리보기, 파일명 자동 매칭 일괄 업로드). `메뉴` 토글은 유지(펼침 시 패널+9개). `gui/quick/` 네임스페이스 분리, 상태 매핑 selected→`selected_idle`(스킵·자동만)·비활성화→`insensitive`, 클릭(press)은 엔진 미지원이라 미출력. test 265→292 · **회귀 0 실측**(기존 구성 5종 57파일 바이트 동일) · renpy 8.5.3 lint 무경고 · **실기 PNG 33개로 실제 실행 스크린샷 검증**(배치시안 일치, 뒤로·빠른불러오기 자동 비활성 확인)
- **원격 스윕 실사용 검증 완료(2026-08-06)** — 사용자가 배포본에서 setup.sql 재실행 후 실제 정리 수행, Storage 파일 수 = 앱 등록 이미지 수 일치 확인(오탐 0·누락 0). 원격 삭제 경로가 실기로 증명됨
- 원격 스윕 유예 기간 선택(7일/1일/전체) — 7일 고정이라 정작 방금 교체한 파일이 전부 가려지던 문제(사용자 보고). 더미 키로 실브라우저 확인 중 **조회 실패가 "정리할 파일이 없습니다"로 보이던 버그** 발견·수정(실패는 `null`, 빈 결과는 `[]` 로 구분 — 둘 다 `[]` 라 호출부가 에러 토스트를 덮어썼다). test 261→265
- 협업 Storage 원격 고아 스윕 신설 — 로컬 삭제 27곳 중 원격 삭제가 0곳이라 버킷이 단조 증가만 하던 비대칭 해소(`collab/assetsGc.ts` + `diffRemoteOrphans` + 에셋 탭 "☁️ 협업 Storage 정리", 모달은 변형 props 로 재사용). 안전장치 2개(모든 방 참조 합집합 + 업로드 후 7일 유예)와 fail-closed(조회 실패 시 중단 — 안 하면 참조 집합이 비어 상대방 에셋 전부를 고아로 표시)가 핵심. `setup.sql` 에 DELETE 정책 추가. test 253→261 · **원격 실사용 검증은 미완**(🎯 참고)
- 고아 에셋 정리에 목록 미리보기 모달 — 개수만 띄우던 `window.confirm` 을 종류별 그룹·썸네일·용량·선택 삭제로 교체(`findOrphanAssets`/`deleteOrphanAssets` 로 조회·삭제 분리). 판정 로직 자체는 원래 정확했고(참조 필드 누락 0), 고아가 UI 에 안 보이는 게 진짜 문제였다. 동반 버그 수정 2건: `clearGeneratedAssets` 가 `mainMenuUi` blob 만 지우고 참조를 남기던 것, 내보내기가 메타 없는 협업 캐시 blob 을 `.npproj.zip` 에서 누락하던 것(기기 이동 시 데이터 손실). test 239→253·실브라우저 시딩으로 6개 그룹·삭제 왕복·0건 경로 검증
- Supertone 잔재 정리 — 코드는 이미 Typecast 전면 이관 완료(61b014f)라 남은 건 문서뿐이었음: 로컬 `docs/`(Supertone API 참고자료, untracked) 삭제 + HANDOFF·types.ts 주석 벤더 중립화. `isTypecastVoiceId` 하위호환 가드는 의도적으로 유지(옛 voiceId 경고). 코드 로직 무변경 → 생성 `.rpy` 회귀 없음
