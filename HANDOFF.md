# HANDOFF

> 살아있는 상태 문서 — **삭제하지 말 것.** 세션 시작 시 CLAUDE.md 워크플로우 규칙대로 정리.
> 상세 이력·완료 내역은 git log가 보존하니 여기엔 남기지 않는다(짧게 유지).

## 🎯 다음 할 일
- [ ] **Typecast 실키 검증**(키 발급 완료) — 앱에 키 입력 후: ① 보이스 목록 자동 로딩+필터(성별/나이/용도) ② ✨ 자연어 추천(한국어/영어 쿼리 — 스키마 방어적 파싱이라 실응답 확인 필요) ③ 미리듣기 1회 생성+캐시 재청취 ④ 💡 견적 ↔ 실제 크레딧 차감 일치 ⑤ 잔여 크레딧 표시(/v1/users/me/subscription) ⑥ 일괄 생성 소량 + 402 가드. 대본 확정 후 3000줄 본생성은 Lite($15/월 20만 크레딧) 전환 검토
- [ ] Vercel 배포 시 api/typecast.ts Edge 함수 배포 확인(기존 api/supertone.ts 삭제됨)

## ✅ 방금 반영됨 (다음 세션에서 git log 확인 후 이 줄들 삭제)
- Supertone→Typecast TTS 전면 이관(서비스 종료 대응) — typecastProvider/프록시/견적(1자=1크레딧 즉시)/VoiceLab 감정·강도/키·문구 전부 교체, 402 가드·배치·이어받기 보존 — test 90/90·외부 빌드·키없음 UI 스모크 통과 (main 병합·푸시 완료)
- VoiceLab 보이스 브라우저 — 목록 자동 로딩(모듈 캐시)+필터(성별/나이/용도)+✨ 자연어 추천(/v1/voices/recommendations), 캐릭터 카드 보이스 설정 모달화+요약 칩(voiceName 저장) — test 100/100·외부 빌드·키없음 스모크 통과 (main 병합·푸시 완료)
- 병합 미리보기 라인 단위 상세화(수정/표기수정/삭제·음성폐기·번역유실·승인해제·배경연결끊김) + 띄어쓰기·부호만 바뀐 줄은 음성 승계 + 성능(SceneCard memo·보이스배치 단일커밋·RenpyTab 지연) + 저장실패 상시배너·협업 수신실패 표면화 — Playwright 4시나리오 검증
- 자동 번역이 줄마다 비어 있는 언어만 요청(기존 검수 번역 덮어쓰기 방지) — Playwright로 요청 언어 실측 검증
- 의상 자동 배정: 배경 이름 키워드 규칙(에셋 탭 캐릭터 카드, 더 구체적인 키워드 우선) + 장면 카드 예외 지정, 재분석 시 예외 승계 — test 118/118·renpy 8.5.3 lint 통과·Playwright 전 경로 검증
- 1080p GUI 스케일 복원(gui.scale 배율 누락 수정 — 720p 수치가 그대로 쓰이던 문제) + 대화창 화면 1/4 고정 + 캐릭터 프레이밍 확대·크기 슬라이더 + 스프라이트 투명여백 트림 — test 123/123·실제 렌파이 렌더 스크린샷으로 270px/25% 측정·lint 통과
- 병합 미리보기가 태그 변경(#끝·#점프·선택지·#복장·#연출)을 감지 — 승인이 잘못 유지되던 버그 동반 수정(연출만 바뀌면 승인 유지) + 게임 퀵메뉴에 '숨기기' 버튼(CG 감상, 기존 h키·가운데클릭과 동일 동작) — test 132/132·Playwright #끝 시나리오·renpy lint 통과
- 타이틀·메뉴 배경 업로드 전용화(Canvas 자동 아트·menuArtStyle 제거, 에셋 탭에 섹션 신설, 미리보기가 업로드본 반영, fit=cover) + '감상한 CG' 갤러리(persistent.cg_seen, 라이트박스 아이템과 공용) — test 140/140·실제 렌파이 렌더로 메인메뉴·갤러리 확인·lint 통과
