# HANDOFF

> 살아있는 상태 문서 — **삭제하지 말 것.** 세션 시작 시 CLAUDE.md 워크플로우 규칙대로 정리.
> 상세 이력·완료 내역은 git log가 보존하니 여기엔 남기지 않는다(짧게 유지).

## 🎯 다음 할 일
- [ ] **ESC(게임 중) 메뉴·설정 화면도 이미지화** — 메인 메뉴와 같은 업로드 구조로 확장(슬롯: 기록·저장·불러오기·설정·메인메뉴·돌아가기 + 창/패널 배경 PNG). 사용자가 해당 이미지를 만든 뒤 착수
- [ ] **Typecast 실키 검증**(키 발급 완료) — 앱에 키 입력 후: ① 보이스 목록 자동 로딩+필터(성별/나이/용도) ② ✨ 자연어 추천(한국어/영어 쿼리 — 스키마 방어적 파싱이라 실응답 확인 필요) ③ 미리듣기 1회 생성+캐시 재청취 ④ 💡 견적 ↔ 실제 크레딧 차감 일치 ⑤ 잔여 크레딧 표시(/v1/users/me/subscription) ⑥ 일괄 생성 소량 + 402 가드. 대본 확정 후 3000줄 본생성은 Lite($15/월 20만 크레딧) 전환 검토
- [ ] Vercel 배포 시 api/typecast.ts Edge 함수 배포 확인(기존 api/supertone.ts 삭제됨)

## 📌 알아둘 것 (지속)
- **Supabase Storage 경고는 무시**(대시보드 "Remove policy" 절대 누르지 말 것 — 에셋 동기화가 400으로 깨진다). 에셋 버킷의 실제 노출 범위는 "배포 URL 아는 사람 = 전부 열람·업로드 가능"이며 감수한 선택(2026-08-05). 상세는 `supabase/setup.sql` 머리 주석.
- 미착수(이번에 의도적으로 뺌): `store.ts`(2073줄) 슬라이스 분리, 탭 컴포넌트 코드 스플리팅.

## ✅ 방금 반영됨 (다음 세션에서 git log 확인 후 이 줄들 삭제)
- 최적화 라운드 — 죽은 코드 제거(미사용 export·배럴 재수출·섀도잉) + 감사 중 발견한 런타임 버그 5건 수정(object URL 누수, 미리보기가 `Character.side` 무시, 장면 제목 개행이 `script.rpy` 깨뜨림, 메뉴아트 blob 유실 시 없는 파일 참조, 폰트 전면 실패 시 실행 불가 zip → DejaVuSans 폴백) + 성능(셀렉터 O(N²) 제거·필드 단위 구독·에셋 URL ref-count 캐시·IDB 배치 읽기·병합 O(n²) 제거·Supabase 지연 로딩으로 엔트리 680→470KB) + 테스트 190→239개(`zip-asset-invariant` 신설, 절대 실패 못 하던 단언 12개 교체, `tests/fixtures.ts`). **생성 `.rpy` 회귀 0**(12개 구성 바이트 동일)·renpy lint 무경고
- 메인 메뉴 배치 프리셋 5종(좌측·하단 가로·우측 2줄·우측 마커·렌파이 기본) + 슬롯별 문구 편집·메뉴 주/부 글꼴·글자 외곽선 + 프리셋 반영 미리보기 — test 190/190·lint 5종 무경고·실게임 5종 스크린샷 검증(기존 구성 회귀 0). ▶ 이모지 치환·하단 가로 오버플로·텍스트 대비 등 실기 전용 버그 6건 동반 수정
- 대사창 그라데이션 개선 — smoothstep 곡선(경계선 제거)·페이드 높이 45%로 확장(글자 위치 불변)·하단 진하기 상한 85%·색은 테마 대사창 색 상속·인앱 미리보기가 실제 출력 반영·새 프로젝트 기본 켜짐 — test 170/170·lint 통과·실게임 스크린샷 2종(밝은/어두운 테마) 검증
- 메인 메뉴 GUI 이미지 업로드(타이틀 로고 + 버튼 6슬롯×3상태, 파일명 자동 매칭 일괄 업로드·배치 조절·1920 기준 미리보기) + 갤러리 허브 화면 — test 155/155·renpy 8.5.3 lint 통과·실물 PNG로 실게임 스크린샷 검증(호버 8px 이동·이어하기 비활성화 확인). `focus_mask` 히트박스 붕괴 함정 발견·회피
