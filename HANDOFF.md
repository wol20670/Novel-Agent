# HANDOFF

> 살아있는 상태 문서 — **삭제하지 말 것.** 세션 시작 시 CLAUDE.md 워크플로우 규칙대로 정리.
> 상세 이력·완료 내역은 git log가 보존하니 여기엔 남기지 않는다(짧게 유지).

## 🎯 다음 할 일
- [ ] **ESC 메뉴 사용자 눈으로 확인**(권장 첫 순서) — 앱에서 zip 뽑아 실행해 10화면을 `카페테리아_ESC메뉴_GUI\미리보기`와 대조. 색이 아트와 안 맞으면 **에셋 탭 ESC 섹션 "글자색"** 5롤(본문/제목/강조/보조/선택배경)에서 조절(코드 수정 불필요). 좌표가 틀어져 보이면 `ESC_LAYOUT`(`src/renpy/gui/screensRpy.ts`, 1920 기준 px)만 고치면 된다
- [ ] **ESC 메뉴 남은 시안 차이(선택 — 읽는 데 지장 없어 의도적으로 뺌)** — ① 시안 제목은 명조인데 게임은 나눔고딕(ESC 제목 폰트 슬롯을 붙이면 해결, `guiOverrides.bodyFontId`·`menuFontId` 선례) ② 기록 화면 행 간격이 `gui.history_height` 탓에 시안보다 넓다(줄이면 긴 대사가 잘릴 위험)
- [ ] **표정 AI 배정 실키 검증** — OpenAI 키 넣고 장면 2~3개로: ① 후보 밖 라벨이 안 나오는지 ② 한 대화에서 표정이 매 줄 튀지 않는지(연속성 지시) ③ 미소 계열 6종이 상황에 맞게 갈리는지 ④ 견적 ↔ 실제 토큰 사용량. 실기 스프라이트 24종 업로드 후 실행 스크린샷까지

## 📌 알아둘 것 (지속)
- **Supabase Storage 경고는 무시**(대시보드 "Remove policy" 절대 누르지 말 것 — 에셋 동기화가 400으로 깨진다). 에셋 버킷의 실제 노출 범위는 "배포 URL 아는 사람 = 전부 열람·업로드 가능"이며 감수한 선택(2026-08-05). 상세는 `supabase/setup.sql` 머리 주석.
- **이미지 GUI 3종(메인·퀵·ESC)은 전부 opt-in** — 아무것도 안 올리면 생성 `.rpy`가 기존과 바이트 단위로 같아야 한다(회귀 0). 손댈 때마다 작업 전 커밋에서 여러 구성으로 `.rpy`를 덤프해두고 `diff -r`로 증명할 것(CLAUDE.md "출력 회귀 0 증명법").
- **TTS(Typecast)는 최후순위로 연기**(2026-08-09) — 실키 검증·Vercel Edge 배포 확인 모두 당분간 안 한다. 코드는 이미 들어와 있으니 재개할 땐 `src/config/aiConfig.ts`·`api/typecast.ts` 부터.
- **메뉴 아트는 언어별로 만들지 않는다**(2026-08-09) — 글자가 구워진 버튼이 영어·일본어에서도 한글로 남지만 감수. 다국어는 **텍스트 번역 + 폰트 교체**로만 간다(Ren'Py `tl/<언어>/` 이미지 치환은 CLAUDE.md에 방법만 남겨둔다).
- 미착수(계속 의도적으로 뺌): `store.ts`(2857줄) 슬라이스 분리, 탭 컴포넌트 코드 스플리팅.

## ✅ 방금 반영됨 (다음 세션에서 git log 확인 후 이 줄들 삭제)
- **장면 도중 `#BGM` 을 그 위치에서 재생**(`ae95cde`). 대본에서 태그가 나온 자리에 `{kind:'bgm'}` 마커 라인을 남기고 generate 가 거기서 `play music` — `#CG` 위치 마커와 똑같은 계약(마커 없는 옛 데이터·장면 맨 앞 지정은 장면 시작 재생으로 폴백, 회귀 0). 곡 교체 시 장면 분할은 그대로. test 393→401 · 회귀 0 실측 · lint 무경고(중간 BGM 샘플까지). ⚠️ **저장된 프로젝트는 대본을 다시 분석해야 마커가 생긴다**.
- **ESC 저장 화면·좌측 내비를 배치시안에 맞춤**(`b83c04e`). ① 빈 슬롯 글자를 썸네일 칸 **안 가운데**로 올리고 칸 아래 캡션은 `슬롯 N`/날짜(`%Y.%m.%d · %H:%M`)로 분리 — `fileSlotsBody()`(`preferencesBody` 와 같은 분기 계약) ② `페이지 N`을 제목 옆 절대좌표로 ③ 내비 pill 에 `xminimum/yminimum` + `selected_left_padding`(점 자리) + 글자 축소. 좌표는 전부 `ESC_LAYOUT` 상수. test 389→393 · **회귀 0 실측**(ESC 미사용 9구성 바이트 동일) · lint 무경고(ESC 23롤 전체 켠 변형까지). **실기에서만 확인 가능한 3건** — `슬롯 [slot]` 치환이 "슬롯 2"로 나오는지 · `page_label` 음수 `ypos`가 안 잘리는지 · `·`(U+00B7)가 두부로 안 나오는지. 어긋나면 `ESC_LAYOUT` 상수만 조정.
- **타이틀 정보·크레딧·도움말 링크 표시 토글**(`31de3e1`, 기본=표시). 이 셋은 `MAIN_MENU_SLOTS`에 없어 이미지 버튼 슬롯 자체가 없다 — 이미지 메뉴를 쓰면 혼자 맨 텍스트로 겉돈다(실측 확인). 끄면 타이틀에서만 사라지고 ESC 메뉴(`screen navigation`)엔 그대로. 에셋 탭 메인메뉴 섹션 체크박스(`mainMenuUi.showInfoLinks`). test 386→389 · **회귀 0 실측**(10구성 108파일 바이트 동일) · lint 무경고. **실기 확인만 남음** — 타이틀에 링크가 없고 버튼 아래 빈 여백도 안 남는지, ESC엔 그대로인지.
