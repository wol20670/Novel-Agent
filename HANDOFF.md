# HANDOFF

> 살아있는 상태 문서 — **삭제하지 말 것.** 세션 시작 시 CLAUDE.md 워크플로우 규칙대로 정리.
> 상세 이력·완료 내역은 git log가 보존하니 여기엔 남기지 않는다(짧게 유지).

## 🎯 다음 할 일
- [ ] **Typecast 실키 검증**(키 발급 완료) — 앱에 키 입력 후: ① 보이스 목록 자동 로딩+필터(성별/나이/용도) ② ✨ 자연어 추천(한국어/영어 쿼리 — 스키마 방어적 파싱이라 실응답 확인 필요) ③ 미리듣기 1회 생성+캐시 재청취 ④ 💡 견적 ↔ 실제 크레딧 차감 일치 ⑤ 잔여 크레딧 표시(/v1/users/me/subscription) ⑥ 일괄 생성 소량 + 402 가드. 대본 확정 후 3000줄 본생성은 Lite($15/월 20만 크레딧) 전환 검토
- [ ] Vercel 배포 시 api/typecast.ts Edge 함수 배포 확인
- [ ] **메인/퀵메뉴 언어별 이미지 업로드** — 글자가 구워진 기존 버튼 아트가 영어·일본어에서 한글로 남는다. Ren'Py `tl/<언어>/` 자동 치환을 쓰면 화면 코드 변경 0, 언어 탭 + zip 배치 경로만 추가하면 된다(안 올린 언어는 원본 폴백). 사용자 확정(2026-08-08)
- [ ] **ESC 메뉴 사용자 눈으로 확인**(권장 첫 순서) — 앱에서 zip 뽑아 실행해 10화면을 `카페테리아_ESC메뉴_GUI\미리보기`와 대조. 색이 아트와 안 맞으면 **에셋 탭 ESC 섹션 "글자색"** 5롤(본문/제목/강조/보조/선택배경)에서 조절(코드 수정 불필요). 좌표가 틀어져 보이면 `ESC_LAYOUT`(`src/renpy/gui/screensRpy.ts`, 1920 기준 px)만 고치면 된다
- [ ] **ESC 메뉴 남은 시안 차이(선택 — 읽는 데 지장 없어 의도적으로 뺌)** — ① 시안 제목은 명조인데 게임은 나눔고딕(ESC 제목 폰트 슬롯을 붙이면 해결, `guiOverrides.bodyFontId`·`menuFontId` 선례) ② 기록 화면 행 간격이 `gui.history_height` 탓에 시안보다 넓다(줄이면 긴 대사가 잘릴 위험) ③ 저장 화면의 "페이지 N"이 시안은 제목 옆인데 지금은 위 가운데
- [ ] **표정 AI 배정 실키 검증** — OpenAI 키 넣고 장면 2~3개로: ① 후보 밖 라벨이 안 나오는지 ② 한 대화에서 표정이 매 줄 튀지 않는지(연속성 지시) ③ 미소 계열 6종이 상황에 맞게 갈리는지 ④ 견적 ↔ 실제 토큰 사용량. 실기 스프라이트 24종 업로드 후 실행 스크린샷까지

## 📌 알아둘 것 (지속)
- **Supabase Storage 경고는 무시**(대시보드 "Remove policy" 절대 누르지 말 것 — 에셋 동기화가 400으로 깨진다). 에셋 버킷의 실제 노출 범위는 "배포 URL 아는 사람 = 전부 열람·업로드 가능"이며 감수한 선택(2026-08-05). 상세는 `supabase/setup.sql` 머리 주석.
- **이미지 GUI 3종(메인·퀵·ESC)은 전부 opt-in** — 아무것도 안 올리면 생성 `.rpy`가 기존과 바이트 단위로 같아야 한다(회귀 0). 손댈 때마다 작업 전 커밋에서 여러 구성으로 `.rpy`를 덤프해두고 `diff -r`로 증명할 것(CLAUDE.md "출력 회귀 0 증명법").
- 미착수(계속 의도적으로 뺌): `store.ts`(2857줄) 슬라이스 분리, 탭 컴포넌트 코드 스플리팅.

## ✅ 방금 반영됨 (다음 세션에서 git log 확인 후 이 줄들 삭제)
- **ESC 메뉴를 배치시안에 맞춤 — 팔레트·레이아웃**(`65c63a7`). 사용자가 실측 11장 ↔ 시안 10장을 대조해 "틀린 부분이 굉장히 많다"고 보고한 건. **이미지 버튼 교체는 하지 않기로 결론**(안 보이던 글자의 대부분이 세이브 날짜·대사 기록·페이지 번호 같은 **동적 텍스트**라 이미지로 못 만든다 — 추가 에셋 요청 불필요, 기존 23장으로 충분). 원인은 지난 커밋이 *버튼* 글자색만 밝은 아트에 맞추고 나머지 팔레트를 어두운 배경 전제로 남겨둔 것.
  - 글자색 → `escMenuUi.colors` 5롤, 에셋 탭에서 조절(기본값=밝은 아이보리 아트) · 좌표 → `ESC_LAYOUT` 단일 소스로 콘텐츠를 배경 아트의 카드 안에 · 설정은 그룹마다 카드(업로드한 9slice가 이제 실제로 보인다) · 갤러리·저장 캡션은 칸 안 좌하단
  - test 373→386 · **회귀 0 실측**(10구성 113파일 바이트 동일) · lint 무경고 · **실기 23장으로 10화면 전부 캡처해 시안과 1:1 대조**
  - 실기에서만 잡힌 결함 4건(전부 CLAUDE.md 함정에 반영): 제목 `ypos` 누락으로 카드 밖에 걸침 · `fit "contain"`+`pos` 로 그림이 왼쪽 쏠림 · 팝업 버튼이 글자 폭으로 쪼그라듦 · `{a=}` 링크가 분홍으로 남음
